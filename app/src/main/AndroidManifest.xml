<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tech.jeremy.zxingtest">

      <application
          android:allowBackup="true"
          android:icon="@mipmap/ic_launcher"
          android:label="@string/app_name"
          android:roundIcon="@mipmap/ic_launcher_round"
          android:supportsRtl="true"
          android:theme="@style/AppTheme"
          tools:ignore="GoogleAppIndexingWarning">
            <activity android:name=".GenerateCode128Activity">
            </activity>
            <activity android:name=".SimpleOneDActivity">
            </activity>
            <activity android:name=".GeneratePdf417Activity">
            </activity>
            <activity android:name=".SimplePdf417Activity">
            </activity>
            <activity android:name=".GenerateQRCodeActivity">
            </activity>
            <activity android:name=".SimpleQRCodeActivity">
            </activity>
            <activity android:name=".MainActivity">
                  <intent-filter>
                        <action android:name="android.intent.action.MAIN"/>

                        <category android:name="android.intent.category.LAUNCHER"/>
                  </intent-filter>
            </activity>
      </application>

</manifest>